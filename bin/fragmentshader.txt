varying vec4 out_color;
varying vec3 out_tc;

uniform samplerCube diff_texture;

void main (void)  
{   
	vec4 diff_map = textureCube(diff_texture, out_tc); 
	if(gl_FrontFacing)
		gl_FragColor = diff_map;  
	else
		gl_FragColor = diff_map;
}    